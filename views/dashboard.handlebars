<p>My Events</p>

{{#each events as |event|}}
<div class="col-3" backgroundImage:`url(${{{event.image_url}}})`>
    <h4 class="">{{event.name}}</h4>
    <p>{{event.description}}</p>
    <hr>
    <p class="mb-0">Location: {{event.location}}</p>
    <p class="mb-0">Date: {{event.date}}</p>
    <p class="mb-0">Time: {{event.time}}</p>
    <p class="mb-0">Created by: {{user.username}}</p>
    <br>
    <br>
</div>
{{/each}}

<p>RSVP Events</p>

{{#each rsvp as |userEvent|}}
<div class="col-3" backgroundImage:`url(${{{userEvent.image_url}}})`>
    <h4 class="">{{userEvent.name}}</h4>
    <p>{{userEvent.description}}</p>
    <hr>
    <p class="mb-0">Location: {{userEvent.location}}</p>
    <p class="mb-0">Date: {{userEvent.date}}</p>
    <p class="mb-0">Time: {{userEvent.time}}</p>
    <p class="mb-0">Created by: {{user.username}}</p>
    <br>
    <br>
</div>
{{/each}}